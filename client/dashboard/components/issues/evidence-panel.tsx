import { FeedbackSourceIcon } from "@/components/feedback/feedback-source-icon";
import { TrendChart } from "@/components/charts/trend-chart";
import type { FeedbackItem, TrendPoint } from "@/lib/types";

export function EvidencePanel({ feedback, trend }: { feedback: FeedbackItem[]; trend: TrendPoint[] }) {
  return <div className="grid gap-5 xl:grid-cols-[minmax(0,1.05fr)_minmax(24rem,.95fr)]"><section className="rounded-panel bg-surface"><header className="flex items-center justify-between border-b border-line px-5 py-4"><div><h2 className="text-base font-bold text-ink">Player evidence</h2><p className="mt-1 text-sm text-muted">184 mentions sampled across four channels <span className="text-critical">+71%</span></p></div></header><div className="divide-y divide-line">{feedback.map((item) => <article key={item.id} className="p-5"><div className="flex items-center justify-between"><FeedbackSourceIcon source={item.source} /><span className="font-mono text-xs text-faint">{item.createdAt} · {item.build}</span></div><blockquote className="mt-3 text-base leading-6 text-ink">“{item.excerpt}”</blockquote></article>)}</div></section><section className="rounded-panel bg-surface p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-base font-bold text-ink">Telemetry correlation</h2><p className="mt-1 text-sm text-muted">Failure rate rose from 19.4% to 34.2% <span className="text-critical">+76%</span></p></div><span className="rounded-full bg-critical-surface px-2 py-1 font-mono text-xs font-bold text-critical">1.8.0 regression</span></div><div className="mt-4 rounded-control bg-surface-sunken p-2"><TrendChart data={trend} compact /></div><p className="mt-4 text-sm leading-6 text-muted">The feedback spike begins within six hours of build 1.8.0 and tracks the boss failure curve with 92% confidence.</p></section></div>;
}
